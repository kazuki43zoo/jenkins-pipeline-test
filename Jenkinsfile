pipeline {
  stages {
    stage('build') {
      echo 'build artifact.'
    }
    stage('unit-test') {
      echo 'perform unit test.'
    }
    stage('integration-test') {
      echo 'perform integration test.'
    }
    stage('deploy') {
      echo 'deploy artifact.'
    }
  }
}
